import { themeStyles as defaultStyles } from '../default/themeStyles';

/**
 * @experimental Dark theme is only a proof of concept, and is not intended to be used in production.
 */
export const themeStyles: typeof defaultStyles = {
  ...defaultStyles,
};
