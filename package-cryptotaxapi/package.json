{
  "name": "@bitwala-cryptobank-squad/package-cryptotaxapi",
  "description": "CryptoTax API wrapper client",
  "license": "SEE LICENSE IN LICENSE",
  "publishConfig": {
    "registry": "https://github.bitwa.la/_registry/npm"
  },
  "version": "1.1.17",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf build",
    "prepare": "yarn build",
    "test": "yarn test:lint",
    "test:lint": "eslint 'src/**/*.{ts,tsx}' --color -c ./.eslintrc.js",
    "build": "yarn clean && tsc"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "querystring": "^0.2.1"
  },
  "devDependencies": {
    "@bitwala/eslint-config": "0.4.2",
    "@types/node": "14.11.5",
    "jest": "^27.5.1",
    "typescript": "4.0.3"
  }
}
