export enum EventNouns {
  URL = 'url',
  TRANSFER = 'transfer',
}

export enum EventVerbs {
  VERIFY = 'verify',
  OBSERVED = 'observed',
  REVERTED = 'reverted',
}

export enum ActionTypes {
  URL_VERIFY = 'url.verify',
  TRANSFER_OBSERVED = 'transfer.observed',
  TRANSFER_REVERTED = 'transfer.reverted',
}
