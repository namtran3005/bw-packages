{
  "name": "@bitwala-cryptobank-squad/package-icons",
  "publishConfig": {
    "registry": "https://github.bitwa.la/_registry/npm"
  },
  "version": "1.1.15",
  "description": "Shared package for UI icons",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf build",
    "prepare": "yarn build",
    "test": "yarn test:lint",
    "test:lint": "eslint 'src/**/*.{ts,tsx}' --color -c ./.eslintrc.js",
    "build": "yarn clean && tsc"
  },
  "peerDependencies": {
    "react": "^17.0.0"
  },
  "devDependencies": {
    "@bitwala/eslint-config": "0.4.2",
    "@types/react": "17.0.3",
    "jest": "^27.5.1",
    "react": "17.0.2",
    "typescript": "4.0.3"
  }
}
