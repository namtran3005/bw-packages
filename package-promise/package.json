{
  "name": "@bitwala-cryptobank-squad/package-promise",
  "description": "Promise utils",
  "license": "SEE LICENSE IN LICENSE",
  "publishConfig": {
    "registry": "https://github.bitwa.la/_registry/npm"
  },
  "version": "1.2.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf build",
    "prepare": "yarn build",
    "test": "yarn test:lint",
    "test:lint": "eslint 'src/**/*.{ts,tsx}' --color -c ./.eslintrc.js",
    "build": "yarn clean && tsc"
  },
  "dependencies": {
    "retry": "0.12.0"
  },
  "devDependencies": {
    "@bitwala/eslint-config": "0.4.2",
    "@types/node": "14.11.2",
    "@types/retry": "0.12.0",
    "jest": "^27.5.1",
    "typescript": "4.0.3"
  }
}
